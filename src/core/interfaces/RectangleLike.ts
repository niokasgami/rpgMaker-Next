/**
 * A rectangle-like object that contains x, y, width, and height properties.
 * @example const rect = { x: 0, y: 0, width: 100, height: 100 };
 */
export type RectangleLike = { x: number, y: number, width: number, height: number };
